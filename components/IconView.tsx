import styled from "@emotion/native";

const IconView = styled.View`
  background-color: ${(props) => props.theme.colors.iconBackgroundColor};
  width: 46px;
  height: 46px;
  border-radius: 9999px;
`;

export default IconView;
